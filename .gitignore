node_modules/
dist/
.env
.env.local
.DS_Store
*.log

# Generated briefs (keep the committed samples in samples/output, ignore ad-hoc runs)
out/

# Never commit the raw key file
Parallel Keys.md

# Private interview material — call transcripts, notes, read-outs. NEVER push to the public repo.
Calls/
